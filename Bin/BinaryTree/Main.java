import Node.*;
import test.*;
public class Main {
    public static void main(String[] args) {
        new _mainUI(true);
        // new Binary();
        // new BinarySearch();
    }
    // del *.class && cd Node && del *.class && cd .. && javac Main.java && java Main
}